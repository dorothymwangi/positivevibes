# Database connection settings
spring.datasource.url=jdbc:mysql://localhost:3308/positive-vibes-data
spring.datasource.username=positive-vibes-data
spring.datasource.password=vibes

# Specify the DBMS
spring.jpa.database = MYSQL

# Show or not log for each sql query
spring.jpa.show-sql = false

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

spring.http.multipart.max-file-size=128KB
spring.http.multipart.max-request-size=128KB

upload.file.directory=C:/Users/Wambui/Liftoff/positive-vibes/src/main/java/org/launchcode/positivevibes/upload
upload.file.extensions=jpg,jpeg,gif,png

